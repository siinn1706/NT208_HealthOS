# Contract Drift — Last Test Run

Total non-device findings: **10**

| Category | Path | Method | Detail |
|----------|------|--------|--------|
| missing-in-contract | `/health/ready` | `GET` | Present in runtime, absent in contract |
| missing-in-contract | `/v1/plans` | `GET` | Present in runtime, absent in contract |
| missing-in-contract | `/v1/plans` | `POST` | Present in runtime, absent in contract |
| missing-in-contract | `/v1/plans/{plan_id}` | `DELETE` | Present in runtime, absent in contract |
| missing-in-contract | `/v1/plans/{plan_id}` | `GET` | Present in runtime, absent in contract |
| missing-in-contract | `/v1/plans/{plan_id}` | `PATCH` | Present in runtime, absent in contract |
| missing-in-contract | `/v1/plans/{plan_id}/archive` | `POST` | Present in runtime, absent in contract |
| missing-in-contract | `/v1/users/me/onboarding-draft` | `DELETE` | Present in runtime, absent in contract |
| missing-in-contract | `/v1/users/me/onboarding-draft` | `GET` | Present in runtime, absent in contract |
| missing-in-contract | `/v1/users/me/onboarding-draft` | `PUT` | Present in runtime, absent in contract |